import React from 'react';

const Header: React.FC = () => {
	return (
		<header className="text-center mb-8">
			<h1 className="text-3xl font-bold text-gray-900 mb-2">Chicago Trail Search</h1>
			<p className="text-gray-600">Find trails near Chicago with natural language search</p>
		</header>
	);
};

export default Header;
